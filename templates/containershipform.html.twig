{{ form_start(form) }}
<div class="row">
    {% if form.containership is defined %}
    <div class="col">
        {{ form_row(form.containership) }}
    </div>
    {% endif %}
    <div class="col" id="some-custom-id">
        {{ form_row(form.name) }}
    </div>
</div>
{{ form_end(form) }}
